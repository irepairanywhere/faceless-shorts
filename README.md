# faceless-shorts

Faceless kinetic-typography Shorts/Reels (the "InsiderForce" look): off-white dot-grid canvas,
words popping in one by one in sync with an AI voiceover, oversized key words, icons/screenshots
dropping in with shadows, slow camera push-ins, orange starburst decor, low music bed, watermark.

## Public template vs. client work
This repo is public. Client-specific material is gitignored and stays on Ahmed's machine only:
the per-client A-roll ad compositions (`remotion/src/*Ad.tsx`, `ARollKit.tsx`, `themes.ts`),
their footage (`remotion/public/aroll-ad/`) and client project folders (`projects/ea-*`,
`projects/immigration-consult-vsl`). Those register through a local-only entry point,
`src/index.client.ts` -> `RootClient.tsx`; the committed `src/index.ts` -> `Root.tsx` exposes
only `KineticShort`. Render A-roll ads with `src/index.client.ts`, everything else with
`src/index.ts` (which is what `make_short.py` uses). Never commit a client name, a client's
footage, or a client project folder to this repo.

## One command
    python3 scripts/make_short.py projects/<slug>            # -> projects/<slug>/final.mp4
    python3 scripts/make_short.py projects/<slug> --preview  # half-res, fast
    python3 scripts/make_short.py projects/<slug> --no-render && (cd remotion && npx remotion studio src/index.ts)

## Make a new video
1. Copy `projects/9router/` to `projects/<slug>/`, drop any screenshots into `projects/<slug>/images/`.
2. Edit `beats.json`: each beat = `vo` (what the voice says) + `lines` (what appears on screen).
   - `{"text": "...", "size": "sm|md|lg|xl", "box": true, "color": "#8E8E8E"}` — wrap words in `*...*` to make them big+bold.
     On-screen words are matched in order to the spoken words, so each word pops when it is said.
   - `{"icon": "github|gemini|kimi|glm|bookmark|claude|claude_confused|starburst", "w": 200, "rotate": -12}`
   - `{"image": "file.png", "w": 900, "tilt": -3, "blur": 3}`  (from projects/<slug>/images/)
   - `{"card": "claude|repo", "w": 760, "title": "...", "subtitle": "..."}`
   - `{"diagram": "bridge|tree", "nodes": ["gemini","kimi","glm"], "left": "claude"}`
   - `{"terminal": ["$ command (typed out)", "✓ green result line", "plain line"], "w": 900, "title": "claude"}` — dark terminal window, lines appear 0.5s apart
   - `{"chat": "message text", "who": "you|ai", "w": 860}` — chat bubble (dark for you, white for Claude)
   - `{"phone": "screen.png", "w": 460}` — phone mockup around a screenshot
   - `{"badge": "1"}` or `{"badge": "icon:github", "size": 170}` — dark circle with a number or logo (badge icons resolve brands too: `icon:googleads`)
   - `{"emoji": "rocket" | "🚀" | "money bag", "w": 260, "rotate": -8}` — **3D emoji sticker** (Microsoft Fluent 3D PNG, 1,285 of them, MIT; index in `remotion/public/emoji-index.json`; staged on demand from `~/Developer/fluentui-emoji`). `"animated": true` uses a **Google Noto animated emoji** instead (411 names in `remotion/public/animated-emoji-names.json`, e.g. fire, mind-blown, eyes, party-popper, money-with-wings; the .webm is downloaded on first use into `remotion/public/animated-emoji/`, CC BY 4.0). Stickers float/tilt on noise; `"float": false` pins them.
   - `{"icon": "claude" | "openai" | "gemini" | "kimi" | "deepseek" | "tiktok" | "googleads" | "wordpress" | …}` — **real brand logos**: unprefixed names try `@lobehub/icons-static-svg` (AI/LLM logos, color variant when one exists), then `simple-icons` (3,459 brands, CC0, brand hex; black logos flip to white on dark beats), then Lucide. Force a set with `lobe:`, `si:` or `lucide:`. Colored copies land in `remotion/public/brand/`.
   - `{"gif": "mind blown", "w": 640}` — **reaction GIF** from Klipy (free key in `.env` as `KLIPY_API_KEY`; fetched as a looping mp4 into `projects/<slug>/stock/`). Social track only — Klipy's terms want media served from their URLs and GIF copyright sits with the creators, so keep them out of paid ads.
   - `{"tierboard": {"add": {"label": "Backlinks", "grade": "S"}}, "w": 960, "ratio": 0.15, "at": 0, "appear": "Backlinks.", "land": "S."}` — **tier-list board** (v9). Rows S/A/B/C/F (override with `"tiers"`), colored tier cells. Chips placed in earlier beats carry over automatically (override with `"placed": [...]`); the `add` chip pops up large over the board centre when the `appear` word is spoken, hovers, then springs into its row on the `land` word and the tier cell flashes. `{"tierboard": {}}` shows the board as it stands (first appearance slides the rows in). Keep the board at `"at": 0` on continuity beats with `transition: cut` and `camera: {"zoom": [1, 1]}` so it never jumps; `ratio` = row height ÷ width (default 0.115). Re-adding a label that is already on the board pulses it instead (hook variants).
   - `{"bar-chart": {"data": [{"label": "Us", "value": 90}, {"label": "Them", "value": 40}]}}` — **comparison bars** (Onda, v10). Largest value gets the theme accent color, the rest are dimmed; labels/track auto-match the beat's ink so they stay legible on light AND dark themes (Onda's own defaults are dark-canvas-only — don't pass `color`/`trackColor`/`barColor` unless overriding on purpose). `max` caps the scale (default 100). Do not set `placement` — omit it so the beat's own flex column sizes the chart (Onda's placement wrapper has no explicit width, which collapses the bars to zero width).
   - `{"browser-frame": {"url": "site.com"}, "w": 700, "height": 400}` — a dark browser-chrome mockup (Onda, v10) wrapping an optional `src` image (from `projects/<slug>/images/`) or nothing (shows the url as a placeholder). Always dark chrome regardless of theme, same convention as `terminal`. No `placement` (same reason as bar-chart).
   - `{"device-frame": {"device": "phone" | "laptop"}, "w": 380}` — a phone or laptop bezel (Onda, v10) wrapping an optional `src` image. Use `phone` for an alternative to the existing bespoke `{"phone": ...}` mockup; `laptop` is new (no other laptop mockup exists). No `placement`.
   - `{"cursor": {"fromX": 0.25, "fromY": 0.8, "toX": 0.65, "toY": 0.35}}` — an animated mouse pointer (Onda, v10) that travels between two canvas-fraction points and clicks on arrival (`"click": false` to disable). Full-canvas overlay — pairs with `browser-frame`/`device-frame`/`code`/`terminal` for "look, it clicks this" moments. Positions are always canvas fractions (0..1), not beat-relative.
   - image extras: `"reveal": true` (blur-to-sharp, default), `"pan": [90, -90]` (camera slides across an oversized shot; use w 1200 + a 1280×560 crop)
   - text extras: `"emphColor": "#E5744C"` colors the *emphasized* words; `"silent": true` = on-screen only (not in the VO), words stagger in after the previous line and consume no spoken words — REQUIRED for any label/box that isn't said out loud, otherwise the matcher derails
   - `{"gap": 40}`; timing overrides for non-text lines: `"at": 0.5` (beat-relative s) or `"after": "GitHub"` (a spoken word)
   - beat options: `layout: center|top`, `grid: true|false`, `deco: true` (starbursts), `camera: {zoom:[1,1.08], pan:[x,y]}`, `transition: zoom|whip|cut` (entry move; alternate whip/zoom for variety)
3. Top-level: `voice_id` (ElevenLabs), `speed` (0.7–1.2), `watermark`, `music: {prompt, volume}` (ElevenLabs Music; delete to skip),
   `theme` (paper | midnight | bold | film | terminal) and `anim` (pop | typewriter | rise | fill). Beats can override `anim`, and set
   `bg: {"stock": "pexels query", "overlay": 0.55}` (film look; fetched from Pexels, 1080×1920 looped; falls back to **Pixabay** when Pexels has nothing, or force it with `"source": "pixabay"` — needs `PIXABAY_API_KEY`) or `bg: {"color": "#hex"}` (a dark color beat on paper now flips text to white automatically).
   Element `{"clip": "pexels query", "w": 760}` drops a stock video card inline. `transition: wipe` = full-frame color sweep (bold default).

### Themes
| theme | look | default anim / transition | decor |
|---|---|---|---|
| paper | off-white, Inter, orange starbursts, dot grid | pop / zoom | starbursts when `deco: true` |
| midnight | RLM brand dark #0B0B11, gradient emphasized words, Inter Tight | rise / whip | drifting gradient orbs |
| bold | yellow/black alternating beats, Anton uppercase, hard wipes | rise / wipe | none (the wipes are the decor) |
| film | Pexels video background, dark gradient overlay, Bebas Neue uppercase, grain + vignette | rise / cut | grain |
| terminal | black, Menlo, green cursor, scanlines | typewriter / cut | scanlines |

### Speakable numbers (rule)
The TTS reads "$1,800" and "$3.11" badly. Write currency and awkward figures in words in `vo` ("under two thousand dollars", "just over three dollars each", "over seventeen thousand") and show the digits on screen as a `silent` line with `"after": "<spoken word>"` so it lands on the right word. Plain counts (535, 174) are fine as digits.

### Variety log (never repeat theme + opening back-to-back)
Rules from Ahmed (2026-09-06): no starbursts in the first two beats and at most one starburst beat per video (enforced in KineticShort); open on the subject, not decoration. SFX: same file never within 0.8s, capped per video (ding 2 / tick 3 / shutter 3 / others 4), terminals tick twice max.
| video | theme | opening hook | anim |
|---|---|---|---|
| 9router / made-by-text / video-use / 5-mcp | paper | text + starbursts | pop |
| proof-ad | bold | cold-open "$51,000" slam, wipes | rise |
| intake-ad | film | ringing-phone stock video + "1 IN 3" | rise |
| geo-ad | midnight | chat thread (question → ChatGPT answer) + count-up | rise + fill |
| google-preferred | paper | badge/logo reveal (Google badge drops in, sentence follows) + hand-drawn circle | pop |
| upgrades-itself | paper | plain typed install command, no decor; each beat switches on one upgrade (slide → leak → annotations → fit → icons → lottie → code → sound) | pop |
| law-marketing-vsl (16:9, 114s) | midnight + royal-blue color beats (#1B2CF5) + 7 stock beats | footage + "give me two minutes" reframe, no decor | rise |
| viral-ranked (A5) | paper + one footage CTA beat | cold-open "65×" + animated mind-blown emoji; variant `oneword` opens on the typed phrase | pop |
| seo-tierlist (idea 5, Sep 9 scan) | bold (yellow hook + CTA, black board beats) | F-stamp: a giant purple F row slams down, three chips drop into it on "three / things / that's", wipe to the full board (the "S to F" slide-in and the llms.txt cold open were both rejected) | rise |

Voice + word timings come from ElevenLabs `/with-timestamps` (cached by text hash — edit the VO and it re-synthesizes).
Rendering is Remotion (React) in `remotion/`; the look lives in `remotion/src/theme.ts` and `remotion/src/components/`.
Requires: node 18+, ffmpeg, python3 + requests, ELEVENLABS_API_KEY (env, ./.env, or ~/Developer/video-use/.env).

## Changelog (every batch should add one upgrade)
- 2026-09-14 v13 — **batch 2 of the A-roll ads (5 finals) + per-client theming.** `ARollKit.tsx` grew a `Theme` context (`useTheme`, `useCard`, `<ARollShell theme={…}>`) so shared cards/captions/end card recolor per client; `themes.ts` holds a second immigration client (palette converted from the site's oklch tokens, Playfair Display + Source Sans 3 installed via Homebrew casks). New shared graphics: `TitleCard`, `CheckList`, `YearsCounter`, `ChipRow` kind `warn` + 3-chip rows, `StepsCta note` (struck line). New ad files: `ClientACitizenshipAd` (4-part takeover with tagged rows), `ClientAMarriageAd` (`cues.hookB` picks the A/B opening; shared body), `OlcClosedAd` (case folder whose flap lifts CLOSED → REOPENED), `OlcDeniedAd` (before → after rows, options-shrinking card). Props builder `~/Desktop/5 ads - batch 2/edit/ad/build_props.py` derives **every** cue from Scribe word times by phrase lookup (`W.at("phrase", after=t)`) — no hand-typed times; it also drops any zoom window under 0.25s (a punch that starts after its own end throws `inputRange must be strictly monotonically increasing` mid-render). `render.sh` is bash-3.2 safe (macOS has no `declare -A`). Lessons: keep row tags under the row title, not beside it; a card should not appear more than ~0.3s before its first word; hide counters until the count word; end card 0.45s after the last word so CTA step 2 gets read time; check every stock clip on a contact sheet (Russian passport, protest crowds and a party lit like police lights all came back for innocent queries).
- 2026-09-13 v12 — **`ARollAd` composition: real talking-head footage instead of a voiceover.** New `remotion/src/ARollAd.tsx` (registered in `Root.tsx`) takes a filmed A-roll plus a props JSON: timed B-roll cutaways (zoom-blur entrance + Ken Burns, light navy tint so stock matches), punch-in zooms (`zooms[]` with a per-zoom origin so top cards never collide with the head), takeover graphics where the speaker's video morphs into a gold-ringed picture-in-picture bubble (one `<Video>`, animated scale/translate/`clip-path`), word-highlight captions from the Scribe transcript (≤3 words, never ending on a filler word, spelling fixes like UCIS→USCIS applied in captions only), music bed + hand-placed SFX. Graphics are bespoke per ad (hook progress bar with PAST DUE stamp, notification toast, court chip, "Mandamus" definition card, decision chips, name card, USCIS receipt-notice mock with the prefix circled, numbered CTA card, chevrons, end card). Text stays in the Reels-ad safe zone; captions sit on the chest (y≈1300), not the mouth. Props builder + review scripts live with the footage (`~/Desktop/clean no subtitiles/edit/ad/build_props.py`, `review.sh`). Render: `npx remotion render src/index.client.ts ARollAd out.mp4 --props=props.json --crf=16` (35s ≈ 47s). **Gotchas:** stock search results lie about location — "houston skyline" returned Pexels "seattle-in-motion"; check the Pexels `url` slug before using any city shot. ElevenLabs Music added a rhythmic low pulse despite "no drums" in the prompt; high-pass the bed at 200 Hz twice so nothing reads as a thud. Music at `musicVolume` 0.3 lands ≈17 LU under a -14 LUFS voice. First ad: an immigration client, mandamus (35.3s, captioned + caption-free). **v12b (same day):** split into `src/ARollKit.tsx` (shell: A-roll/zoom/PiP/B-roll with `trim`/captions/chevrons/end card/audio + reusable `ChipRow`, `Stamp`, `NameCard`, `StepsCta`, `Hl`, `TakeoverBg`) and one file per ad with only its bespoke graphics: `ARollAd.tsx` (mandamus), `NotarioAd.tsx` (swinging NOTARIO PÚBLICO sign + STOP, "Same word. Different job." compare takeover, FILED WRONG / MISSED / DENIED stamps), `RelativesAd.tsx` (3-kinds hook with green-card mock, spouse/parent/child list takeover, Work permit → Green card path, language pills). **Music removed from A-roll ads** (client feedback: no music; a real person giving advice reads more trustworthy dry) — `music` is optional, SFX kept light.
- 2026-09-10 — **second client track: a home-services contractor.** New skill `faceless-shorts-exterior-alliance` (`~/.claude/skills/`) with `BRAND.md`, `HOOKS.md` and `IDEAS.md`. Projects are prefixed `ea-` so client work never collides with RLM work. The research finding that shaped it: **pure kinetic typography does not appear in any breakout in the roofing niche** — what wins is annotated real footage. So this track runs footage-first (`film`, `bg.stock` on most beats, text as hook overlay + captions rather than the substance), which is the opposite weighting from the RLM videos. First video: `projects/ea-claim-clock` (39.5s, film, 8/9 beats on stock).
- 2026-09-11 v11 — **real job photos as the visual spine.** First EA video built entirely from client-supplied stills instead of Pexels: matched before/after pairs pre-composited to 4:5 with BEFORE/AFTER tags, plus single-angle stills for the reveal beats. Also documented `"underline": false` on a text line — KineticShort auto-draws an emphasis bar under *any* emphasized word on an `lg`/`xl` line, which silently stacks the green underline on every beat; set it false everywhere you don't want it. Video: ea-navy-swap.
- 2026-09-10 v10 — **4 Onda elements wired in: `bar-chart`, `browser-frame`, `device-frame`, `cursor`.** These 14 components were installed 2026-09-08 (v8) but never connected to a beat element until now. Onda components validate/default their own props via zod (`xSchema.parse(...)` called right before render in `KineticShort.tsx` — pass a raw partial object, defaults fill in), and animate off `useCurrentFrame()` directly rather than an explicit `frame` prop, which works here because each beat's `Scene` already runs inside its own Sequence so the hook returns beat-relative frames — same clock as everything else, just read a different way; pass `delay={startF}` and it lines up automatically. **Two real bugs found and fixed while wiring these up, not just my own mistakes:** (1) `ondajs` 0.9.2's own `lib-primitives` bundle re-exports a `MotionBlur`/`MotionTrail` pair from `primitives/index.ts` that its own installer never actually ships (confirmed by re-running `ondajs add` — verified upstream gap, not a local corruption) — broke the build the moment anything imported `Surface` through that barrel; fixed by dropping the dead re-export. (2) Onda's `PlacementBox` wrapper has no explicit width, which silently collapses `BarChart`'s `width: 80%` to zero — bars never painted, only the fixed-width labels did. Fix: omit `placement` entirely on bar-chart/browser-frame/device-frame so the beat's own flex column sizes them, exactly like every other element already does (`PlacementBox` is a documented no-op when `placement` is undefined). Also: Onda's own component defaults assume a dark canvas (e.g. `BarChart`'s label color defaults to near-white `#F2F2F4`), invisible on light themes — `bar-chart`'s dispatch now passes this beat's actual `ink`/`theme.accent` as defaults instead. Verified with `remotion still` frame grabs on all 4 elements before shipping (see `projects/_smoke9/`, kept as a regression fixture like `_smoke`/`_smoke8`). Idea originally scouted from OpenMontage (see [[faceless-shorts-upgrade-backlog]] memory) but this is 100% this repo's own component set — OpenMontage itself was not installed.
- 2026-09-10 — **`qa_variety_check.py`.** Static pre-render check (no deps) that scores a `beats.json` against this README's own rules: 50s cap (VSL-exempt), per-beat pacing (≤6s), starburst placement/count (rule 13), back-to-back/repeated transitions, unspoken digits that should be a silent overlay (rule 9b), and theme+anim repeat vs the last row of the Variety log below. Idea and scoring concept adapted from OpenMontage's slideshow-risk scorer (`github.com/calesthio/OpenMontage`), rewritten against this repo's actual beat schema rather than its generic scene model. Run it as step 1 of QA, before the frame-grab pass. It's advisory — it doesn't know a brand name ("9Router") from a spoken number, so use judgment on its warnings.
- 2026-09-09 v9b — **stamp opener + multi-chip drops.** `tierboard` gained `"stamp": true` (the row slams down from 3.4× like a rubber stamp, white flash on impact, shutter SFX, never a thud) and a list form of `add` (`[{"label", "grade", "on": "<spoken word>"}, …]`: chips drop in from above the row on their own words; chips wrap to a second line inside a row when they do not fit). Used for the seo-tierlist hook after Ahmed rejected the "llms.txt is F tier" cold open ("the first three seconds… we need a stronger hook that's visual as well"): `{"tierboard": {"tiers": ["F"], "stamp": true, "add": [...]}, "w": 960, "ratio": 0.24, "at": 0.3}`. Chips placed in the hook carry into the full board (F row pre-filled) and their own beats pulse instead of flying in.
- 2026-09-09 v9 — **tier-list board.** New `tierboard` element (`remotion/src/components/TierBoard.tsx`): S–F rows with colored tier cells, chips that hover over the board and spring into their row on a spoken word (`appear` / `land` resolve to VO words in the builder), landing flash, placed chips accumulate across beats in `build_timeline.py` (`TIER_STATE`), `pulse` for a chip that is already on the board, `ratio` for row height. SFX: one `pop` per landing (pool + caps apply), `swish` when the board first slides in. First video: `projects/seo-tierlist` (44.9s, bold) + hook variant `llms`. **Gotcha found:** `transition: blur` (zoomBlur) as the SECOND beat's entry on the bold theme rendered the whole first beat blank (verified with `remotion still`; `flip` and `cut` are fine, and blur mid-video in viral-ranked was fine). Until the cause is found, never use `blur` on beat 2.
- 2026-09-09 v8b — **whoosh.mp3 retired** (Ahmed heard it as the thud again at 4s and 29s of viral-ranked; fourth time he has asked). `whoosh.mp3`, `thud.mp3`, `kenney/thud*.mp3` moved to `.retired-sfx/` (outside `public/`), the whoosh family now rotates through Kenney `maximize1/2/3` only, `bong1` dropped from notify, and `collectSfx` has a BANNED regex (`thud|whoosh.mp3|boom|hit|impact|bong`) so no future pool entry can bring a boom back. A bass check (`ffmpeg -af "lowpass=f=180,volumedetect"`, bass peak within ~8 dB of full peak = boom) also retired Kenney `card1 pep1 pep2 drop2-4 bong1 confirm1 metal0/1`; pools are now ding→confirm3/4, pop→drop1/pep3, shutter→card2/3, whoosh→maximize1-3, notify→glass1/2. Rule: any sound with a low-frequency transient is out; when unsure, silence.
- 2026-09-08 v8 — **stickers, logos, GIFs, ten transitions.** `emoji` element (Microsoft Fluent 3D PNG stickers + Google Noto animated emoji via `@remotion/animated-emoji`, both staged on demand), brand logos for `icon`/`badge` (`@lobehub/icons-static-svg` for AI/LLM brands, `simple-icons` for everything else, colored at build time, white on dark beats), `gif` element (Klipy, free Tenor-compatible API), Pixabay as a second stock well (`scripts/fetch_stock.py`), ten new transitions (`flip clock film ripple blur dissolve dreamy` from `@remotion/transitions`, `glass chromatic iris` from Onda), dark `bg.color` beats on paper flip to white ink, and elements placed `after` a beat's last word are pulled in 0.6s so they are actually seen. Also installed: 13 Onda components in `remotion/src/components/onda/` (browser-frame, device-frame, cursor, count-up, slot-machine-roll, bar-chart, confetti, draw-on, mesh-gradient, stat-card…, not wired into beats yet), `@remotion/install-whisper-cpp` (word timings from any audio, unused so far), `vhs` (terminal recordings → mp4), `shot-scraper` 1.10 (`shot-scraper video storyboard.yml --mp4` records real website walkthroughs). Smoke: `projects/_smoke8`. First video: `projects/viral-ranked` (A5).
- 2026-09-07 v6b — **thud/"hammer" SFX retired for good** (Ahmed, third time asking): wipe/push transitions now swish, xl words no longer hit; the file stays in `public/sfx/` but nothing maps to it.
- 2026-09-07 v6 (landscape) — `"aspect": "16:9"` in beats.json renders 1920×1080 for landing-page VSLs: Root/Scene/Grain/Orbs/DotGrid/LeakOverlay read the composition size instead of 1080×1920, text column widens to 1500px and type scales ×1.22, watermark drops to the bottom edge, Pexels fetches landscape footage (`-wide.mp4` cache). Hard 50s cap does not apply to VSLs (first one: `law-marketing-vsl`, 114s, 21 beats).
- 2026-09-06 v1 — 9Router recreation: kinetic text, boxes, icons, cards, bridge/tree diagrams, starbursts, dot grid, music, loudnorm.
- 2026-09-06 v2 — terminal window (typewriter), chat bubbles, phone mockup, logo badges, blur-to-sharp screenshot reveal + camera pan across oversized shots, whip transitions, accent-colored emphasis, starburst drop shadows, number-word matching (forty = 40). Videos: made-by-text, video-use-skill.

- 2026-09-06 v3 (motion + sound) — stronger spring overshoot on every pop, emphasized words punch in from 35% with an accent-color flash and an underline sweep (lg/xl lines), automatic count-up for *emphasized* numbers ≥ 100 (0.7s), lines slide in from alternating sides, elements rotate in, camera drift + 1.08 push-in default, whip = 140px + blur, dot grid drifts, starbursts spin + pulse with shadows. Sound design: ElevenLabs SFX (`remotion/public/sfx/`: whoosh on transitions, pop on emphasized words/elements, tick per terminal line), set `"no_sfx": true` in beats.json to disable. Matcher: 14-word window for a line's first word. `chat` gets `label`; icons `phone`, `phone_missed`, `phone_dark`. Videos: geo-ad, intake-ad, proof-ad.

- 2026-09-06 v4 (variety) — theme system (paper/midnight/bold/film/terminal), word animations (pop/typewriter/rise/fill), wipe transitions, Pexels stock backgrounds + inline clips (`scripts/fetch_stock.py`, key in `.env`), chat labels, silent lines with `after`, 20-word matcher window for a line's first word, 50-second cap.

- 2026-09-06 v5 (pace + sound + hybrid) — voice default 1.1 and automatic dead-air trimming (`tighten`: gaps > 0.42s cut to 0.26s, word times shifted, original kept as voice_raw.mp3); faster pops/entries; SFX library of 8 (whoosh, swish, thud, pop, tick, ding, notify, shutter) mapped by element type, one hit per 0.22s, same sound never twice within 0.45s, `sfx_level` per project; stock footage under ANY theme (`bg.stock` flips text to white, adds grain, keeps the theme's fonts); `hook_variants` + `--hook <name>` render alternate openings into `variants/<name>/` for A/B tests.
- **v6 (2026-09-06) — Remotion skills pack.** Installed `remotion-dev/skills` and rebuilt the scene layer on it: `TransitionSeries` with real presentations (`slide`, `whip`=slide L/R, `fade`, `push`=pushCut, `wipe`, `zoom`=fade + in-scene push-in, `cut`) — each sequence is extended by the next beat's transition length so beats still start on their VO frame; `leak` = `@remotion/effects` light-leak overlay at the cut (full strength before a dark beat, 0.42 on paper); text `fit: true` via `@remotion/layout-utils` fitText (two-pass); `annot: underline|circle|highlight|box|bracket` via `@remotion/rough-notation` (one continuous stroke when the whole line is emphasized); stock/clip video on `@remotion/media` `<Video>` with `bg.start`/`bg.rate`; WebGL2 enabled in `remotion.config.ts` (`angle`). Helpers: `src/components/Fx.tsx`. First video: `projects/google-preferred` (39.7s).
- **v7 (2026-09-06) — libraries.** `icon` now falls back to any **Lucide** icon by kebab name (`{"icon": "mouse-pointer-click", "w": 150, "color": "#fff", "bg": "#E5744C"}`; 1,500+ names at lucide.dev), icons pop with a **motion-blur Trail**; new `lottie` element (`{"lottie": "file.json", "w": 520, "loop": true, "speed": 1}`, files in `remotion/public/lottie/` — drop free LottieFiles JSONs there); new `code` element = syntax-highlighted code window via code-hike lighter (`{"code": [lines], "lang": "html|ts|bash|json|python…", "title": "footer.html", "theme": "github-dark|dracula|one-dark-pro|nord…", "typing": true}`), prefer it over `terminal` for real code; camera drift is now `@remotion/noise` (organic, not sine); SFX rotate through pools (original + CC0 **Kenney** variants in `remotion/public/sfx/kenney/`, from `kapishdima/soundcn`) with per-family caps. Packages: @remotion/lottie, motion-blur, shapes, paths, noise, lucide-react, @code-hike/lighter, codehike. Reference clones: `~/Developer/template-code-hike`, `~/Developer/soundcn`. Smoke test: `projects/_smoke`.

## Hook bank (open differently every time)
| hook | how | best theme |
|---|---|---|
| Cold-open number | silent xl digits at 0.05s, count-up + ding, sentence follows | bold, midnight |
| Footage + one word | `bg.stock` + a single xl word over it ("1 IN 3") | film, any theme |
| Chat thread | two `chat` bubbles (question → answer) then the stat | midnight |
| Typewriter prompt | terminal theme, first line typed with cursor | terminal |
| Color slam | bold theme, solid color beat, xl word + thud, wipe to footage | bold |
| Problem statement | 2-beat variant: pain (footage) → proof (solid) | any |
| Screenshot reveal | blurred `image` sharpening while the hook line lands | paper, midnight |
| Card drop | `card`/`phone` element first, text second | paper |
| Progressive reveal | start deliberately plain (cut transitions, no annotations), then unlock one capability per beat as the voice names it — the video itself is the demo | paper |
| Badge / logo reveal | the product's own badge or logo `image` at 0.05s, the claim lands under it, `annot: circle` on the key word | paper, midnight |

## Transition bank
`slide` (from bottom, whoosh) · `whip` (slide from left/right alternating, whoosh) · `fade` · `push` (pushCut with white flash) · `wipe` (directional wipe) · `zoom` (fade + in-scene push-in, swish) · `leak` (light-leak overlay on the cut, swish — use before a DARK beat; on paper it is only a warm flash) · `cut` (silent).
**v8 additions** — Remotion built-ins: `flip` (3D card flip, alternating side) · `clock` (clock-hand wipe) · `film` (film-burn shader) · `ripple` (water ripple shader) · `blur` (zoom-blur whip, the TikTok "speed ramp" cut — **not as the entry of beat 2**, it blanked the hook beat on bold, see v9) · `dissolve` (burning-edge dissolve) · `dreamy` (dreamy zoom). Onda (copy-paste source in `remotion/src/components/onda/`): `glass` (frosted-glass wipe) · `chromatic` (RGB-split glitch cut) · `iris` (circular iris). Mix at least three per video; never the same one twice in a row. Shader ones need WebGL (already on via `angle`).

## Testing hooks (A/B)
1. Put alternatives in `hook_variants: {"name": beat | [beats]}` (a list replaces the first N beats).
2. `make_short.py projects/<slug> --hook name` renders `projects/<slug>/variants/name/final.mp4` (images/stock shared via symlink).
3. Post both, same caption, 24h apart or as two ad creatives; log in `results.csv` below.

## Results log (`results.csv` — fill after posting)
`date,slug,variant,platform,theme,hook,anim,transition,views_24h,views_72h,avg_watch_pct,comments,saves,leads` — the skill reads this to bias the next batch toward what held attention.
1. `ffmpeg -ss <t>` exact frames at ~60% of every beat → nothing clipped at the bottom (watermark zone starts at y=1650), no overflow.
2. Scribe transcript of final.mp4 vs timeline word times → deltas within ±0.15s.
3. `ebur128` → about -14 LUFS, peak ≤ -1 dBTP.
